<tr>
	<td><input type="checkbox" class="ajax_checkbox" name="merge" data-receiver="{{=it.username}}"></td>
    <td>{{=it.username}}</td>
    <td><button type="button" class="btn btn-danger merge_delete" data-merge="{{=it.merge_id}}">Delete</button></td>
</tr>